# Rewritten by netbox-firstboot.py, and only when an operator-supplied TLS
# certificate is installed. With the generated self-signed certificate HSTS
# would make the browser's warning non-bypassable and lock you out.
