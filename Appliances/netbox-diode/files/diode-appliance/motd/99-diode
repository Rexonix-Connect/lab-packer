#!/bin/sh
# Appliance summary at login. diode-status exits non-zero when a service is
# down, which must not make the MOTD itself fail.
[ -x /usr/local/sbin/diode-status ] || exit 0
echo
/usr/local/sbin/diode-status 2>/dev/null || true
exit 0
